# DevDesk 产品需求基线

> 版本：2.0
> 更新时间：2026-08-20

## 1. 产品定位

DevDesk 是面向个人开发者的本地工作区、项目与开发环境工作台。它解决配置分散和项目入口分散的问题，但不接管源代码，不承担通用终端、完整 Git 客户端、云同步或团队协作职责。

产品围绕三个核心对象组织：

- 工作区：一个本机根目录，以及该目录应使用的 Git/SSH 身份。
- 项目：工作区根目录下的一级目录；更深层工程作为该一级项目的子项目。
- 环境：独立管理 Node 版本、镜像、包管理器、全局包、缓存和路径。

项目可以是 Java、Python、Go、Rust、Node.js 或其他类型。工作区不会判断依赖是否安装，也不提供项目脚本运行器。

## 2. 产品原则

1. 本地优先：业务数据和配置保存在本机，不上传私钥或项目内容。
2. 真实状态：涉及 Git、SSH、nvm、nrm 和系统文件的操作必须验证实际结果。
3. 明确授权：Hosts 接管、删除、清理、安装和导入等操作需要清楚说明影响。
4. 领域隔离：工作区只维护目录和身份关系，Node 能力只属于 Node 模块。
5. 紧凑界面：固定 Codex 浅色风格，不提供主题切换，不使用营销式页面布局。

## 3. 信息架构

一级导航固定为：首页、Hosts、Git、SSH、工作区、模板、Node、设置。

首页只展示欢迎语、时间、系统基本状态和 CPU/内存资源趋势，不展示工作区业务统计、项目命令或依赖状态。

## 4. 核心需求

### 4.1 首页

- 主进程每 30 秒采样 CPU、内存和系统信息。
- 持久化最近 48 个采样点，应用重启后继续展示趋势。
- 少于两个有效采样点时展示积累状态，不绘制伪趋势。
- 欢迎语包含当前用户名和随机鼓励语。

### 4.2 工作区与项目

- 工作区名称和规范化根路径必须唯一。
- 工作区可绑定一个 Git 身份，并可配置子项目扫描深度 1 至 5 层。
- 工作区可配置额外忽略目录；内置忽略依赖、构建产物、虚拟环境和隐藏目录。
- 主列表始终只展示根目录下的一级目录。
- 在一级项目内部递归发现子项目，命中项目标记后停止该分支继续下钻。
- 扫描具有目录预算、截断提示和取消能力。
- 项目和子项目均支持备注；扫描不得覆盖用户备注。
- 支持手动纳入外部目录，移除记录不得删除磁盘目录。
- 支持使用系统文件管理器、Codex、VS Code、Cursor、Windsurf、Zed 和 JetBrains IDE 打开项目。

工作区不提供 JavaScript 依赖检测或安装、`package.json` 脚本执行、跨语言构建或任务日志。

### 4.3 Git 与 SSH

- 管理全局 Git 用户名和邮箱，以及多个可关联 SSH 密钥的 Git 身份。
- 按工作区生成 `includeIf gitdir` 规则和身份 profile。
- 身份详情展示关联工作区、密钥和受管文件快照。
- 工作区可在真实仓库中验证最终生效的 `user.name`、`user.email`、`core.sshCommand` 及来源文件。
- 被工作区引用的 Git 身份不可删除；删除 SSH 密钥前展示关联身份。
- 不在业务数据中持久化私钥内容。

### 4.4 Node 环境

- 直接请求 Node.js 兼容的 `index.json` 获取可安装版本，并提供短期离线缓存。
- 安装、切换和默认版本依赖真实可用的 nvm；镜像切换依赖 nrm。
- 缺少 nrm 时允许在页面明确安装。
- 管理 npm、pnpm、yarn、bun 状态、Registry、全局包和可清理缓存。
- 全局包列表采用延迟加载和缓存，避免阻塞 Node 页面首屏。
- 安装任务展示阶段、进度、日志、失败、取消和重试。
- 自定义下载源默认仅接受 HTTPS，本机回环地址允许 HTTP。
- 保存前可验证索引、安装包和 `SHASUMS256.txt`。
- UI 明确说明同源 SHA256 不能证明镜像发布者可信。

### 4.5 Hosts

- 只维护带 DevDesk 标记的受管区块。
- 首次打开不自动接管系统已有映射。
- 系统映射单独预览，用户确认后才导入受管区块。
- 支持启停、编辑、删除、备份恢复、打开文件、刷新 DNS 和打开域名。

### 4.6 模板

- 支持本地目录模板和 Git 仓库模板。
- Git 克隆使用非交互模式，区分 Git 缺失、凭据、网络、超时和权限错误。
- 创建一级项目或指定一级项目下的子项目时可填写备注。
- 创建失败时回滚目标目录，不删除任何已有目录。

### 4.7 设置与数据

- 固定 Codex 浅色主题，不保留主题持久化字段。
- 支持开机自启、托盘驻留、日志级别和开发者工具开关。
- 支持数据目录迁移、导入、导出、清理和日志管理。
- 数据写入按目标文件串行化，使用唯一临时文件原子替换。
- 损坏 JSON 被隔离为 `.corrupt` 副本。
- 使用显式 schema migration；当前版本为 v2，可导入 v1 备份。

### 4.8 更新与发布

- 打包版本从 GitHub Releases 检查、下载并安装更新。
- Release 流水线必须先通过 lint、typecheck、test、补丁格式和构建入口检查。
- macOS 正式发布必须使用 Developer ID 签名并完成 Apple 公证。
- 每次正式发布至少验证一次旧版本到新版本的升级链路。

## 5. 非功能要求

- Electron 保持上下文隔离、sandbox、禁用渲染层 Node 集成并启用 Web 安全。
- IPC 参数在主进程边界校验，错误统一转换为中文用户信息。
- 长任务不得阻塞窗口线程。
- 默认窗口尺寸下页面避免外层滚动，表头和工具栏保持固定。
- 新增、编辑使用 Sheet/Drawer；危险操作使用 AlertDialog；异步操作必须有加载和结果反馈。

## 6. 验收基线

```bash
pnpm lint
pnpm typecheck
pnpm test
pnpm build
git diff --check
```

macOS Electron 预览应检查首页趋势、工作区递归扫描与取消、Git 身份验证、Hosts 导入预览、Node 下载源测试及设置数据迁移。

## 7. 明确不做

- 云同步和团队协作。
- 插件市场。
- 远程部署和服务器运维。
- 通用终端或完整 Git 客户端。
- 跨语言项目命令运行器。
- 匿名遥测。
