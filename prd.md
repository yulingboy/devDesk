# DevDesk 产品需求文档（PRD）

> 文档状态：V1.0 产品基线。
>
> 更新时间：2026-08-15。
>
> 相关文档：[项目设计文档](./design.md)。

## 1. 产品背景

开发者的本机环境通常由多个彼此独立的工具和配置文件组成：Hosts、Git 身份、SSH 密钥、项目目录、Node 版本、包管理器和镜像等。配置分散会带来以下问题：

- 不同项目使用不同 Git 身份时，切换步骤复杂且容易提交错账号。
- Node 版本、包管理器和镜像状态缺乏统一视图，排查环境问题成本高。
- 工作区、项目模板和本地目录之间没有统一管理入口。
- Hosts、`.gitconfig` 等系统文件修改风险较高，缺少明确反馈和恢复路径。
- 配置迁移依赖手工复制多个文件，难以确认是否完整。

DevDesk 通过单机桌面应用把这些能力集中管理，并保持对真实文件、路径、命令结果和错误信息的可见性。

## 2. 产品目标

### 2.1 核心目标

1. 用户可以在一个应用中完成常见本机开发环境的查看、配置和维护。
2. 用户可以建立 SSH 密钥、Git 身份、工作区和项目之间的稳定关联。
3. 高风险操作必须有确认、错误反馈或恢复机制。
4. Node 安装等长任务不能阻塞应用界面，并应持续展示进度和结果。
5. 用户可以迁移主要业务数据，并能查看实际数据目录。

### 2.2 非目标

- 不提供云端同步、团队协作和账号权限体系。
- 不替代 IDE、完整 Git 客户端或终端。
- 不管理远程主机和部署流水线。
- 不读取或持久化 SSH 私钥内容。
- V1.0 不提供插件市场和第三方扩展机制。

## 3. 用户画像与核心任务

### 3.1 多身份开发者

拥有个人、公司或多个代码托管平台账号，需要让不同项目自动使用正确的 Git 用户名、邮箱和 SSH 密钥。

核心任务：配置密钥与 Git 身份，将身份绑定到工作区，并验证实际生效的配置文件。

### 3.2 Node.js 开发者

同时维护多个 Node 版本和包管理器，经常需要切换版本、镜像、全局包或清理缓存。

核心任务：查看环境状态，安装和切换 Node，管理镜像、包管理器及全局依赖。

### 3.3 多项目维护者

本机有多个工作区和大量项目，希望快速发现项目、查看 Git 状态并从模板创建新项目。

核心任务：建立工作区，扫描项目，从模板创建项目，并用编辑器或文件管理器打开。

## 4. 需求范围与优先级

### 4.1 P0：V1.0 必须交付

- 系统概览与环境检测。
- Hosts 记录管理、备份恢复和 DNS 刷新。
- SSH 密钥发现、录入、生成和删除影响提示。
- Git 全局配置、身份配置和工作区规则同步。
- 工作区管理、第一层项目扫描和项目 Git 状态展示。
- Git/本地模板管理及项目创建。
- Node 版本、镜像、包管理器、全局包和缓存管理。
- 设置、数据目录、导入导出、托盘和开机自启。

### 4.2 发布质量要求

以下要求与 P0 功能同为 V1.0 发布门槛：

- Windows、macOS、Linux 安装包验证。
- 关键系统修改的失败恢复和跨平台错误提示。
- 数据格式版本迁移与兼容性验证。
- 首次使用引导和危险操作说明。

### 4.3 后续版本候选

- 云端或端到端加密的个人配置同步。
- 可配置的项目扫描深度和手动项目注册。
- 网络代理、证书和下载诊断。
- 应用自动更新、崩溃报告和诊断包导出。

## 5. 产品信息架构

一级导航固定为：首页、Host 管理、Git 配置、SSH 密钥、工作区、项目模板、Node 管理、系统设置。

Node 管理包含六个页签：可安装版本、已安装版本、nrm 镜像、包管理器、全局包、环境信息。

系统设置包含五个分区：通用设置、数据管理、高级设置、环境检测、关于。

## 6. 核心用户旅程

### 6.1 建立项目身份环境

```mermaid
flowchart LR
  A[发现或生成 SSH 密钥] --> B[创建 Git 身份]
  B --> C[创建工作区并绑定身份]
  C --> D[扫描或创建项目]
  D --> E[按工作区自动应用 Git 配置]
```

成功标准：用户无需在每个项目内重复执行 `git config`，即可让工作区下的 Git 仓库使用指定身份。

### 6.2 建立 Node 开发环境

```mermaid
flowchart LR
  A[环境检测] --> B[选择 Node 源]
  B --> C[安装 Node 版本]
  C --> D[切换或设为默认]
  D --> E[选择包管理器和镜像]
  E --> F[管理全局包]
```

成功标准：安装任务有可见进度，成功后版本可被识别和切换；失败时展示可执行的错误信息。

## 7. 功能需求

### 7.1 应用框架与全局行为

| 编号   | 需求                                 | 验收标准                                                   |
| ------ | ------------------------------------ | ---------------------------------------------------------- |
| APP-01 | 应用只允许一个主实例运行             | 第二次启动时聚焦或恢复已有窗口，不创建重复实例             |
| APP-02 | 应用提供固定侧边导航和自定义窗口控制 | 所有一级模块可达；最小化、最大化/恢复、关闭状态正确        |
| APP-03 | 支持最小化到系统托盘                 | 开启设置后关闭窗口仅隐藏；托盘可恢复窗口或退出应用         |
| APP-04 | 全局异步操作提供一致反馈             | 成功有提示；失败展示主进程返回的可读错误；重复提交受控     |
| APP-05 | 页面异常不能导致整个窗口白屏         | 页面或根级错误边界展示降级状态，并将渲染错误写入主进程日志 |
| APP-06 | 非 Electron 环境提供明确降级提示     | 未注入 bridge 时不调用系统 API，并说明桌面运行时不可用     |

### 7.2 首页系统概览

| 编号    | 需求                               | 验收标准                                                 |
| ------- | ---------------------------------- | -------------------------------------------------------- |
| DASH-01 | 展示当前时间、日期、主机名和问候语 | 页面打开后立即显示本地时间，主机信息加载后自动补齐       |
| DASH-02 | 展示 CPU、内存和主磁盘使用率       | 数值限制在 0% 至 100%，读取失败时使用可理解的降级值      |
| DASH-03 | 展示设备、系统、运行时和目录信息   | 包含操作系统、架构、CPU、用户、Node、Electron 和主要目录 |
| DASH-04 | 展示磁盘和网络接口列表             | 最多展示 4 个磁盘和 6 个有效网络接口                     |
| DASH-05 | 系统状态由主进程后台更新           | 默认每 30 秒采样；页面先显示最近快照，再接收实时更新     |

### 7.3 Host 管理

| 编号    | 需求                               | 验收标准                                                                               |
| ------- | ---------------------------------- | -------------------------------------------------------------------------------------- |
| HOST-01 | 用户可搜索 Hosts 记录              | 支持按 IP、域名和备注筛选，不修改原始数据                                              |
| HOST-02 | 用户可新增和编辑记录               | IPv4 和域名必填且格式合法；域名大小写不敏感且不可重复；备注最多 120 字                 |
| HOST-03 | 用户可启用、禁用和删除记录         | 禁用记录以注释形式写入；删除需确认；保存使用整表覆盖语义并串行执行                     |
| HOST-04 | 应用通过明确标记的受管区块管理记录 | 已有受管区块时仅替换该区块；首次接管时把可识别的 IPv4 记录迁入受管区块，并保留其他内容 |
| HOST-05 | 首次写入前创建原始文件备份         | 备份存在时不重复覆盖；用户可以恢复备份                                                 |
| HOST-06 | 支持打开 Hosts 文件和刷新 DNS      | 根据平台执行对应操作，权限不足或命令缺失时给出明确错误                                 |
| HOST-07 | 支持复制 IP/域名和访问域名         | 复制成功有反馈；访问操作交给系统默认浏览器                                             |

### 7.4 Git 配置

| 编号   | 需求                             | 验收标准                                                            |
| ------ | -------------------------------- | ------------------------------------------------------------------- |
| GIT-01 | 展示真实全局 Git 配置            | 从本机配置来源读取用户名、邮箱和来源文件；全局项不可删除            |
| GIT-02 | 用户可编辑全局用户名和邮箱       | 用户名非空、邮箱格式合法；保存后回读真实文件快照                    |
| GIT-03 | 用户可新增、编辑和复制 Git 身份  | 配置名称唯一；用户名非空；邮箱合法；SSH 密钥可选且必须存在          |
| GIT-04 | 展示身份关联的 SSH 密钥和工作区  | 列表可显示密钥名称、被哪些工作区使用及托管文件路径                  |
| GIT-05 | 被工作区引用的身份不可删除       | 删除入口禁用或主进程拒绝，并展示最多 3 个关联工作区名称             |
| GIT-06 | 保存身份后同步真实 Git 规则      | 每个身份生成稳定的 profile 文件，工作区通过 `includeIf gitdir` 引用 |
| GIT-07 | 用户可查看全局和托管配置文件内容 | 仅查看不触发写盘；文件缺失时给出重新保存配置的提示                  |

### 7.5 SSH 密钥

| 编号   | 需求                           | 验收标准                                                         |
| ------ | ------------------------------ | ---------------------------------------------------------------- |
| SSH-01 | 自动发现本机 `~/.ssh` 中的公钥 | 只识别合法 `.pub` 文件；相同公钥不重复导入；名称冲突自动生成后缀 |
| SSH-02 | 用户可手动录入或编辑公钥       | 名称和公钥必填；名称唯一；自动补充算法和指纹                     |
| SSH-03 | 用户可生成 ed25519 或 RSA 密钥 | RSA 使用 4096 位；支持注释和可选口令；生成后显示在列表中         |
| SSH-04 | 不持久化私钥内容               | 只保存私钥路径；运行时检查文件是否仍存在并显示缺失警告           |
| SSH-05 | 删除前展示关联影响             | 确认框展示受影响的 Git 配置；删除后解除这些关联并同步 Git 规则   |
| SSH-06 | 支持搜索和复制公钥             | 可按名称、指纹、公钥筛选；复制操作有反馈                         |

### 7.6 工作区与项目

| 编号  | 需求                           | 验收标准                                                          |
| ----- | ------------------------------ | ----------------------------------------------------------------- |
| WS-01 | 用户可新增和编辑工作区         | 名称和根目录必填；名称与规范化路径均不可重复；描述和 Git 配置可选 |
| WS-02 | 用户可删除工作区               | 删除前确认；删除后同步删除该工作区的项目记录和 Git 路径规则       |
| WS-03 | 用户可打开工作区目录           | 使用系统文件管理器打开；路径无效时展示错误                        |
| WS-04 | 用户可扫描工作区项目           | 只扫描根目录第一层的非隐藏目录，单次最多导入 500 个候选目录       |
| WS-05 | 扫描同步新增和失效项目         | 目录新增则创建项目记录；目录删除则移除记录；结果提示新增/移除数量 |
| WS-06 | 展示项目 Git 状态              | Git 项目展示分支和未提交标记；状态读取失败不阻塞项目列表          |
| WS-07 | 支持搜索、打开项目目录和编辑器 | 搜索按名称/路径过滤；编辑器不可用时给出明确提示                   |
| WS-08 | 没有模板时引导用户先创建模板   | 从工作区发起创建项目前刷新模板列表；为空时不打开无效表单          |

### 7.7 项目模板

| 编号   | 需求                                  | 验收标准                                                            |
| ------ | ------------------------------------- | ------------------------------------------------------------------- |
| TPL-01 | 用户可维护 Git 或本地目录模板         | 名称和来源必填；名称唯一；描述最多 200 字；类型仅允许 `git`/`local` |
| TPL-02 | 支持模板搜索、列表/卡片展示和编辑删除 | 搜索按模板信息过滤；删除需确认                                      |
| TPL-03 | 用户可从模板创建项目                  | 必须选择模板和工作区；项目名称不可为空或包含路径特殊字符            |
| TPL-04 | 目标目录必须不存在                    | 发现同名文件或目录时拒绝创建，不覆盖现有内容                        |
| TPL-05 | Git 模板使用非交互式浅克隆            | 禁止凭据弹窗；网络、权限、超时和 Git 缺失错误应转为可执行提示       |
| TPL-06 | 本地模板过滤无关重型目录              | 不复制 `.git`、`node_modules`、构建产物等既定排除项                 |
| TPL-07 | 创建失败时回滚半成品                  | 目标目录原本不存在时，任一步失败都尝试删除已创建内容                |
| TPL-08 | 创建成功后写入项目清单                | 返回新项目并刷新工作区项目列表；Git 模板不保留模板仓库的 `.git`     |

### 7.8 Node 管理

#### 7.8.1 版本与任务

| 编号    | 需求                       | 验收标准                                                         |
| ------- | -------------------------- | ---------------------------------------------------------------- |
| NODE-01 | 展示 Node 环境总览         | 显示当前版本、默认版本、nvm/nrm 状态、当前镜像和警告             |
| NODE-02 | 查询可安装版本             | 支持关键字、全部/LTS/Current 筛选、条数限制和手动刷新            |
| NODE-03 | 安装与当前系统匹配的版本包 | 根据平台和架构选择下载包；版本号必须合法；无可用安装包时拒绝     |
| NODE-04 | 安装任务持续反馈状态       | 状态包含等待、下载、解压、完成、失败、跳过；展示进度、日志和时间 |
| NODE-05 | 安装过程不阻塞主窗口       | 下载、校验和解压在 worker 执行；进度通过任务流实时更新           |
| NODE-06 | 已存在的完整版本跳过安装   | 校验通过则标记为 `skipped`；残缺目录先清理再重新安装             |
| NODE-07 | 用户可切换和设置默认版本   | 操作成功后刷新总览和已安装列表                                   |
| NODE-08 | 当前版本不可删除           | 删除当前版本必须失败并提示先切换；删除默认版本时清空默认值       |

#### 7.8.2 镜像、包管理器和全局包

| 编号    | 需求                            | 验收标准                                                       |
| ------- | ------------------------------- | -------------------------------------------------------------- |
| NODE-09 | 管理 nrm 镜像                   | 支持新增、编辑、删除、测速和切换；名称和 registry URL 必须合法 |
| NODE-10 | 展示 npm/pnpm/yarn/bun 可用状态 | 显示版本、registry 和当前默认项；不可用工具不能执行包操作      |
| NODE-11 | 设置默认包管理器和独立 registry | 保存后刷新状态，并同步到应用设置中的偏好值                     |
| NODE-12 | 管理全局包                      | 支持搜索、安装、卸载、检测过期和单包更新；危险操作需确认       |
| NODE-13 | 展示和清理环境缓存              | 显示 nvm、Node 与缓存路径和大小；清理前确认，成功后刷新快照    |

### 7.9 系统设置

| 编号   | 需求                                         | 验收标准                                                               |
| ------ | -------------------------------------------- | ---------------------------------------------------------------------- |
| SET-01 | 支持 8 种主题色                              | 切换后立即应用，并持久化到设置                                         |
| SET-02 | 支持开机自启和最小化到托盘                   | 正式安装环境中写入系统设置；不支持的平台安全跳过并记录日志             |
| SET-03 | 展示数据目录、占用和 Git 配置数量            | 统计失败时使用安全默认值，不阻断设置页                                 |
| SET-04 | 支持打开和更改数据目录                       | 用户取消不修改状态；更改时先迁移数据，再切换运行时目录                 |
| SET-05 | 支持数据导出和导入                           | 使用带版本号的 JSON 快照；取消选择文件不报错；导入后立即刷新运行时设置 |
| SET-06 | 支持清除缓存和清空业务数据                   | 清空业务数据必须二次确认，且不得删除应用本体和基础设置文件             |
| SET-07 | 支持设置 Node 版本索引、下载源和默认包管理器 | 保存后 Node 管理模块读取最新配置                                       |
| SET-08 | 支持日志级别和开发者工具                     | 日志级别立即生效；开发者工具只能由受信任窗口打开                       |
| SET-09 | 支持重置设置                                 | 二次确认后恢复默认设置，同时刷新运行时状态                             |
| SET-10 | 环境检测逐项执行并实时输出日志               | 支持停止后续检测；展示通过/失败数量、版本和原始安装输出                |
| SET-11 | 对可自动安装的缺失工具提供安装入口           | npm 或官方 shell 方式安装；仅支持指引的工具展示平台相关说明            |
| SET-12 | 关于页展示真实运行时版本                     | 展示应用、Electron、Chrome、Node、构建日期和许可证信息                 |

## 8. 跨模块业务规则

1. SSH 密钥、Git 配置、工作区和项目形成依赖链，任何删除操作都必须处理下游引用。
2. Git 身份或 SSH 密钥变化后，应立即重建工作区 Git 规则。
3. 数据读取操作原则上不得隐式修改磁盘；自动修复或发现结果应由明确写入动作持久化。
4. 系统文件、全局配置、目录删除、包卸载和数据清空属于危险操作，必须有确认或恢复路径。
5. 所有名称、路径和 URL 在主进程再次校验，不能只依赖前端表单校验。
6. 辅助信息加载失败不得阻断主业务，例如项目 Git 状态、缓存大小和系统快照。

## 9. 页面与交互规范

### 9.1 状态要求

- 首次加载使用骨架屏或明确加载状态，不能展示错误的空数据。
- 空列表应说明下一步动作，例如创建、扫描或先配置依赖模块。
- 搜索无结果与数据本身为空使用不同文案。
- 长任务必须显示进行中状态并禁止重复提交。
- 操作失败后保留用户已输入的表单内容。

### 9.2 反馈要求

- CRUD 成功使用轻量消息反馈。
- 破坏性操作使用确认弹窗，并明确影响对象。
- 权限、命令缺失、网络超时、路径不存在等错误必须给出下一步建议。
- 文件来源、实际路径和底层日志在需要排障的页面保持可查看。

## 10. 数据与隐私要求

- 所有业务数据默认只存储在用户本机。
- 不采集代码内容、SSH 私钥内容和远程账号凭据。
- Git 模板拉取不缓存用户名或密码，不允许主进程等待交互式认证。
- 导出文件可能包含公钥、目录结构和本机路径，导出前或首次使用时应提示其敏感性。
- 数据目录写入应采用原子替换策略，避免异常退出导致 JSON 文件截断。

## 11. 非功能需求

### 11.1 性能

- 首屏可交互时间和内存占用目标值需在首轮安装包测试后确定。
- 系统状态采样间隔默认 30 秒，不因单次采样失败持续占用 CPU。
- 项目扫描单次最多处理 500 个第一层目录。
- Node 下载、校验、解压不得运行在渲染线程。

### 11.2 兼容性

- 支持 Windows x64、macOS 和 Linux 主流桌面发行版。
- 支持 x64、arm64；Windows 兼容 ia32 Node 包识别。
- 平台不支持的能力必须降级提示，不能导致应用启动失败。

### 11.3 安全与稳定性

- Electron 必须保持上下文隔离、sandbox、禁用 Node 集成和启用 Web 安全。
- 只接受受管理主窗口的 IPC 调用。
- 外部 URL 一律由系统浏览器打开。
- 未捕获异常和 Promise 拒绝写入本地日志。
- 应用保持单实例运行，并在退出时释放托盘、采样器和流式会话。

## 12. 成功指标

以下指标用于发布验收和质量观测：

| 指标                                    | 目标  |
| --------------------------------------- | ----- |
| 首次环境检测完成率                      | ≥ 95% |
| 常规配置 CRUD 成功率                    | ≥ 99% |
| Node 安装任务完成或给出可诊断失败的比例 | 100%  |
| Hosts/Git 修改失败后的数据可恢复率      | 100%  |
| 页面级未处理白屏                        | 0     |
| 三平台核心流程冒烟通过率                | 100%  |

上述指标通过自动化测试、本地诊断报告和发布前测试矩阵统计；如引入匿名遥测，须先完成隐私评审并获得用户明确授权。

## 13. 发布验收标准

1. TypeScript、ESLint、单元测试和生产构建全部通过。
2. SSH → Git 配置 → 工作区 → 项目的完整链路通过人工和自动化验证。
3. Node 安装、切换、默认版本、删除和失败重试完成至少一轮真实环境验证。
4. Hosts 修改、DNS 刷新和备份恢复在三个目标平台验证。
5. 数据目录迁移、导出、清空和导入恢复形成闭环。
6. 安装包完成启动、单实例、托盘、窗口控制和卸载验证。
7. 任何已知的数据丢失、私钥泄露、任意命令执行或窗口外 IPC 问题均阻断发布。

## 14. 待产品确认

1. Hosts 首次接管非受管 IPv4 记录时，是否需要逐条导入确认。
2. 数据导出是否必须包含模板和环境缓存快照。
3. SSH 密钥生成是否默认要求口令，还是继续允许无口令并仅提示风险。
4. Git 规则同步失败时是否需要事务式回滚配置存储。
5. 项目扫描是否长期固定第一层，是否需要手动添加非第一层项目。
6. 是否允许执行远程 shell 安装脚本，或改为只展示安装指引。
7. 是否引入匿名遥测，以及相应的授权、数据最小化和退出机制。
8. 是否需要应用自动更新、代码签名、公证和崩溃诊断上传。
